# Gotchas

Add only verified traps that are likely to recur, such as a Supabase RLS behavior, an environment constraint, or a test setup prerequisite.

Record the observed behavior, workaround, and proof. Remove or mark entries superseded once the underlying condition changes.
