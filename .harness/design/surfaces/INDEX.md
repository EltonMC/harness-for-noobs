# Surface Briefs

Create one short brief per substantial route or workflow when its user job or visual direction differs from the product default.

Each brief should state the user job, primary action, hierarchy, constraints, critical states, and a link to the work item. Do not duplicate `PRODUCT.md` or `DESIGN.md`.
