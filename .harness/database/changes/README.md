# Database Change Proposals

Create one proposal from `.harness/templates/database-change-proposal.md` before writing a migration. Keep it proportional: an additive lookup table needs a short proposal; a tenancy, deletion, or data migration change needs a complete one.
